version: "3.9"
services:
  backend:
    build: backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    healthcheck:
      test: curl --fail http://localhost:8080/health || exit 1
      interval: 60s
      retries: 5
      start_period: 20s
      timeout: 10s